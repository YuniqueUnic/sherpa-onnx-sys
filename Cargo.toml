# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "sherpa-onnx-sys"
version = "1.13.8"
build = "src/build.rs"
links = "sherpa-onnx"
include = [
    "src/**",
    "Cargo.toml",
    "README.md",
    "LICENSE*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Raw FFI bindings to the sherpa-onnx C API"
readme = "README.md"
keywords = [
    "ffi",
    "speech",
    "sherpa-onnx",
    "bindings",
]
categories = ["external-ffi-bindings"]
license = "Apache-2.0"
repository = "https://github.com/k2-fsa/sherpa-onnx"

[package.metadata.docs.rs]
default-features = false
features = []

[features]
default = ["static"]
shared = []
static = []

[lib]
name = "sherpa_onnx_sys"
path = "src/lib.rs"

# No features also selects static linking, so this dependency is not optional.
# The library imports it only for desktop static builds.
[target.'cfg(any(target_os = "linux", target_os = "macos", target_os = "windows"))'.dependencies.ort-sys]
version = "=2.0.0-rc.13"
default-features = false
features = ["api-28", "download-binaries", "tls-native"]

[build-dependencies.bzip2]
version = "0.4"

[build-dependencies.tar]
version = "0.4"

[build-dependencies.ureq]
version = "2.12"
features = ["proxy-from-env"]

[build-dependencies.zip]
version = "2"

[dev-dependencies.tempfile]
version = "3"
